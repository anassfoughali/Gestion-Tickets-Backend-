server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/Foutouh/conffig-repo.git
          default-label: main
          clone-on-start: true